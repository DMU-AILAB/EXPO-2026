# VisionGuide 인수인계 문서

> 최종 업데이트: 2026-07-02  
> 작업자 인계 시점 브랜치: `feat/pi-roi-web-editor`

---

## 현재 상태 요약

### 완료된 작업

| 구분 | 내용 |
|------|------|
| PC 시뮬레이터 | ROI 폴리곤 편집, 한글 오버레이(PIL), 지팡이 하단 10% 감지 구간, 오디오 트리거 |
| Pi ROI 웹 에디터 | FastAPI + HTML5 Canvas 서버 구현 (`roi_editor/`) |
| Pi 파일 배포 | `camera_live_pi.py`, `detect.py`, `edgetpu_infer.py`, `roi_editor/`, `simulator/`, TFLite 모델 |
| Pi 패키지 설치 | shapely, fastapi, uvicorn, pydantic, opencv, ai-edge-litert, pillow, numpy 모두 설치 완료 |
| Makefile | Python 경로 변수화(`PI_PYTHON`, `PI_PIP`), 배포 자동화 타겟 완성 |

### 미완료 — 바로 이어서 할 일

1. **현재 브랜치 PR 머지** — `feat/pi-roi-web-editor` → `main` (Makefile 변경사항 포함)
2. **camera_live_pi.py Pi 포팅 이슈** (GitHub Issues #11~#15)
3. **다중 ROI 충돌 처리** (Issues #16~#17)
4. **관리자 대시보드** (Issues #18~#21)

---

## Pi 접속 정보

| 항목 | 값 |
|------|-----|
| 현재 IP | `192.168.0.89` (교내 2.4GHz WiFi) |
| 와이파이 변경 시 | Pi에서 `hostname -I` 로 새 IP 확인 후 아래 명령어로 덮어쓰기 |
| SSH 접속 | `ssh ailab@192.168.0.89` |
| 기본 작업 디렉토리 | `~/visionguide/` |

> **참고**: 핫스팟으로 변경 시 IP가 172.20.10.10 등으로 바뀝니다. `hostname -I` 로 항상 확인하세요.

IP 바뀐 경우 Makefile 기본값 수정:
```
# Makefile 17번째 줄
PI      ?= <새IP>
```
또는 매번 `make <target> PI=<새IP>` 로 지정.

---

## Pi 현재 설치 상태

```
~/visionguide/
├── camera_live_pi.py       ← Pi 카메라 추론 + MJPEG 스트리밍
├── detect.py
├── edgetpu_infer.py
├── roi_editor/
│   ├── server.py           ← ROI 웹 에디터 FastAPI 서버
│   └── static/index.html   ← 브라우저 Canvas UI
├── simulator/
│   ├── __init__.py
│   └── roi_manager.py
└── runs/white_cane_v1-2/weights/best_int8.tflite
```

**설치된 Python 패키지** (Python 3.13, user 설치):

| 패키지 | 버전 |
|--------|------|
| ai-edge-litert | 2.1.5 |
| opencv-python-headless | 4.13.0.92 |
| shapely | 2.1.2 |
| fastapi | 0.138.2 |
| uvicorn | 0.49.0 |
| pydantic | 2.13.4 |
| pydantic-core | 2.46.4 ← 이 버전 고정 필요 |
| pillow | 11.1.0 |
| numpy | 2.2.4 |

> **중요**: `pydantic-core`는 반드시 `2.46.4`여야 합니다. `pip install pydantic` 하면 자동으로 2.47.0으로 올라가므로 주의.

---

## 서비스 실행 방법

### 1. ROI 웹 에디터만 실행 (추천 첫 테스트)

```bash
ssh ailab@192.168.0.89
cd ~/visionguide
python3 roi_editor/server.py
```

브라우저에서 `http://192.168.0.89:5000` 접속.

### 2. 카메라 앱 + ROI 에디터 동시 실행 (tmux)

```bash
ssh ailab@192.168.0.89
tmux new -s visionguide

# 창 1: 카메라
cd ~/visionguide
python3 camera_live_pi.py --headless --port 8080 --roi-config rois.json

# Ctrl+B, C (새 창)

# 창 2: ROI 에디터
cd ~/visionguide
python3 roi_editor/server.py

# Ctrl+B, D (세션 분리)
```

- 카메라 스트림: `http://192.168.0.89:8080/stream.mjpg`
- ROI 에디터: `http://192.168.0.89:5000`

### 3. Makefile 사용 (PC에서)

```bash
# 연결 확인
make ping

# 파일만 재전송 (코드 수정 후)
make sync
make sync-roi-editor

# Pi에서 실행
make run-headless       # 카메라 앱
make run-roi-editor     # ROI 에디터
```

---

## 알려진 문제 / 주의사항

### pywheels 차단 문제
Pi의 `/etc/pip.conf`에 piwheels.org가 등록되어 있지만 네트워크에서 접근이 안 됩니다.
`pip install` 시 **반드시** `PIP_CONFIG_FILE=/dev/null` 환경변수를 붙여야 합니다:

```bash
PIP_CONFIG_FILE=/dev/null pip3 install --break-system-packages <패키지>
```

또는 sudo가 있으면 아예 제거:
```bash
sudo sed -i '/extra-index-url/d' /etc/pip.conf
```

### Pi 인터넷 속도 매우 느림 (2.4GHz WiFi 기준)
패키지 다운로드가 13KB/s로 극도로 느립니다.
**핸드폰 핫스팟** 연결 시 정상 속도 (현재 핫스팟 연결 상태).
새 패키지 설치가 필요하면 PC에서 wheel 다운로드 후 scp로 전송하는 방식 권장:

```bash
# PC에서 aarch64 wheel 다운로드
pip download --platform manylinux_2_17_aarch64 --python-version 313 --only-binary=:all: <패키지> -d C:\tmp\pi_wheels\

# Pi로 전송
scp C:\tmp\pi_wheels\*.whl ailab@172.20.10.10:~/pi_wheels/

# Pi에서 로컬 설치
PIP_CONFIG_FILE=/dev/null pip3 install --break-system-packages --no-index --find-links ~/pi_wheels/ <패키지>
```

### rois.json 없음
아직 `~/visionguide/rois.json`이 없습니다. ROI 에디터 실행 후 ROI를 추가하면 자동 생성됩니다.
또는 PC의 `rois_example.json`을 복사해서 시작:
```bash
scp rois_example.json ailab@172.20.10.10:~/visionguide/rois.json
```

### MJPEG 스트림 Content-Length 픽스 (완료)
`camera_live_pi.py`의 MJPEG 스트리밍에서 각 프레임에 `Content-Length` 헤더가 누락되어 브라우저가 1~2 프레임 후 멈추는 버그가 있었습니다.  
수정 완료 — `~/visionguide/camera_live_pi.py`에 반영, 현재 Pi에서 포트 8080으로 정상 스트리밍 중.

### camera_live_pi.py 미포팅 기능
현재 Pi의 `camera_live_pi.py`에는 시뮬레이터에서 작업한 아래 기능들이 **반영되지 않았습니다**:
- ROI 하단 10% 감지 구간 (Issue #11)
- PIL 한글 오버레이 (Issue #12)
- rois.json 핫리로드 연동 (Issue #13)

---

## 남은 GitHub Issues

### Pi 포팅 (우선순위 높음)

| Issue | 내용 |
|-------|------|
| #11 | `camera_live_pi.py` — ROI 하단 10% 감지 구간 적용 |
| #12 | `camera_live_pi.py` — PIL 한글 오버레이 적용 |
| #13 | `camera_live_pi.py` — rois.json 핫리로드 연동 |
| #14 | GPIO 릴레이 트리거 구현 |
| #15 | watchdog.py 헬스 모니터링 |

### 다중 ROI 처리

| Issue | 내용 |
|-------|------|
| #16 | 우선순위 선착 + 글로벌 쿨다운 방식 |
| #17 | 순차 큐 방식 (최대 2개) |

참고: 아이디어 정리 문서 → `docs/multi-roi-collision-ideas.md`

### 관리자 대시보드 (나중에)

| Issue | 내용 |
|-------|------|
| #18 | FastAPI 백엔드 스켈레톤 |
| #19 | JWT + API Key 인증 |
| #20 | ROI 관리 API + WebSocket |
| #21 | React 프론트엔드 |

---

## 브랜치 현황

| 브랜치 | 상태 | 내용 |
|--------|------|------|
| `main` | 최신 | PR #22 (python310) 머지됨 |
| `feat/pi-roi-web-editor` | **현재 작업 브랜치** | ROI 웹 에디터 구현, Makefile 수정 — **PR 미완료** |
| `feat/pi-deploy-automation` | 구 브랜치 | 이전 배포 자동화 (참고용) |

### 현재 브랜치 커밋 목록 (main 대비)
```
222945c4 docs: Pi 배포 가이드 전면 업데이트
e7ec4daf feat: Pi ROI 웹 에디터 구현 및 배포 자동화
```

### 다음 할 일 (커밋/PR)
```bash
# Makefile 변경사항 커밋 후 PR
git add Makefile
git commit -m "fix: Pi IP 기본값 업데이트 및 Python 경로 변수화"
git push origin feat/pi-roi-web-editor

# GitHub에서 main으로 PR 생성
gh pr create --title "feat: Pi ROI 웹 에디터 및 배포 자동화" --base main
```

---

## 참고 문서

| 파일 | 내용 |
|------|------|
| `docs/pi-deployment-guide.md` | Pi 배포 전체 흐름 (Step 0~4) |
| `docs/multi-roi-collision-ideas.md` | 다중 ROI 처리 아이디어 6가지 |
| `CLAUDE.md` | 프로젝트 전체 구조 및 개발 가이드 |
| `rois_example.json` | ROI 설정 파일 예시 |
