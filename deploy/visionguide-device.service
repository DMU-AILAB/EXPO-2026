[Unit]
Description=VisionGuide White Cane Detection (headless, MJPEG :8080)
After=network.target sound.target
Wants=sound.target

[Service]
Type=simple
User=__USER__
Environment=PYTHONUNBUFFERED=1
# Pi 4 native USB 2.0/3.0 ports share a power group.  Current deployment does not use TPU.
Environment=VISIONGUIDE_USB_AUDIO_HUB=1-1
Environment=VISIONGUIDE_USB_AUDIO_SETTLE=0.3
Environment=VISIONGUIDE_USB_AUDIO_ALWAYS_ON=1
WorkingDirectory=/home/__USER__/visionguide
ExecStart=__PI_PYTHON__ /home/__USER__/visionguide/camera_live_pi.py --headless --port 8080 --max-fps 10 --camera-config /home/__USER__/visionguide/camera_config.json --roi-config /home/__USER__/visionguide/rois.json --model-variant v10_320 --inference-backend tflite
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
